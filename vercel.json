{
    "version": 2,
    "functions": {
        "api/notes.mjs": {
            "runtime": "nodejs18.x"
        }
    },
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/api/(.*)",
            "dest": "/api/$1"
        },
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}